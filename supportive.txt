Совпадающие имена классов в styleC.css и styleA.css:
html - стили свопадают полностью, замена не нужна
.page-header - замена на .page-headerA
 И по этому в клиентских страницах в header выводим header с классом .page-header
  в админских - ApplicationLogo компонент.

Вырезано из app.blade.php
<link rel="preconnect" href="https://fonts.bunny.net">
        <link href="https://fonts.bunny.net/css?family=figtree:400,500,600&display=swap" rel="stylesheet" />
http://webdevdip/build/assets/app-eed483c4.css


Из SectionAdminLayout: { flag === 1? <Popup flag={flag1}/>: null }
из Popup
{data.length?
              data.length ===1?
                <label className='conf-step__label conf-step__label-fullsize' htmlFor={data[0].name}>
                  {data[0].nameInput}
                  <input className='conf-step__input' name={name} placeholder={data[0].placeholder} required/>
                </label> :
                <InputDatas data={data}/>:
              <p className='conf-step__paragraph'>Вы действительно хотите удалить зал <span
                /* В span подставить название зала*/
              ></span>?</p>
            }
из InputDatas
export default function InputDatas({data}) {
  data.map((item, index)=> {
    return (
      <label key={index} className='conf-step__label conf-step__label-fullsize' htmlFor={item.name}>
        {item.nameInput}
        <input className='conf-step__input' name={item.name} placeholder={item.placeholder} required/>
      </label>  
    )
  });
}
